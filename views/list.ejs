<!DOCTYPE html>
<html>
  <head>
    <title><%= context.title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= context.user.name %>님, 친구를 선택하세요</h1>
    <form action="/map" method="post">
      <table border="1">
        <thead>
          <tr>
            <th></th>
            <th>학번</th>
            <th>이름</th>
          </tr>
        </thead>
        <tbody>
          <% context.friends.forEach(function (obj) { %> 
            <% if (obj.id != context.user.id) { %>
              <tr>
                <td><input type="checkbox" name="relations" value=<%= obj.id %> <%= obj.checked %> /></td>
                <td><%= obj.id %></td>
                <td><%= obj.name %></td>
              </tr>
            <% } %> 
          <% }) %>
        </tbody>
      </table>
      <br>
      <button type="submit">친구 지도 보기</button>
    </form>
    <br>
    <a href="/"> << 뒤로가기</a>
  </body>
</html>
